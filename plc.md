# 繁易 FC5 PLC Modbus TCP 连接 Nano 配置指南

## 一、网络拓扑

```
PLC (FC5) ──网线──→ 交换机 ←──网线──→ Nano
  192.168.2.200                   192.168.2.100
```

两台设备必须在同一网段，通过交换机互通。

---

## 二、Nano 侧（已就绪，无需操作）

- 协议：Modbus TCP Server（从站）
- IP：`192.168.2.100`
- 端口：`502`
- 从站 ID：`1`

### 寄存器映射表

| Holding Register | FC5 地址格式 A | FC5 地址格式 B | 方向 | 含义 |
|:---:|:---:|:---:|:---:|---|
| HR0 | 0 | 40001 | PLC → Nano | 触发：写 1 触发拍照（Nano 消费后清 0） |
| HR1 | 1 | 40002 | Nano → PLC | 结果：0=空闲，1=OK，2=NG |
| HR2 | 2 | 40003 | Nano → PLC | nano状态：0=未就绪/故障，1=就绪 |
| HR3 | 3 | 40004 | Nano → PLC | 完成标志：Nano 写好 HR1 后置 1，收到应答后清 0（PLC 端只读） |
| HR4 | 4 | 40005 | PLC → Nano | 应答：PLC 取走 HR1 后写 1（PLC 端只写） |

> 数据类型：16 位无符号整型（uint16_t），范围 0~65535。

> **握手时序（核心，替代定时猜结果）**：PLC 不需要"等 300ms 再读结果"。
> Nano 检测完成写 HR1 后置 HR3=1；PLC 每轮扫表看到 HR3=1 就读 HR1，
> 取走后写 HR4=1 应答；Nano 看到 HR4 0→1 边沿就清 HR3、复位 HR4，解锁下一触发。
> HR3=1 期间 Nano 会拒绝新触发（防止 HR1 被下一板覆盖）。
> 只要 HR3 没变 1，PLC 就继续扫下一轮，不存在"错过"窗口。
>
> **为什么拆成两个寄存器（HR3 完成 / HR4 应答）**：如果对同一个寄存器又建读标签又建写标签，
> 读轮询每周期会把寄存器值刷回本地变量，覆盖程序刚写的应答 → HR3 永远钉 1，整条线卡死。
> 拆开后 HR3 上 PLC 只读、HR4 上 PLC 只写，方向互不冲突。

---

## 三、FC5 侧 FStudio 配置步骤

### 步骤 1：设置 FC5 本机 IP

1. 打开 FStudio，连接 FC5 PLC
2. 左侧项目树 → **系统配置** → **以太网**
3. 设置 FC5 自己的 IP：

| 参数 | 值 |
|------|-----|
| IP 地址 | `192.168.2.200` |
| 子网掩码 | `255.255.255.0` |

> IP 末位可自定义，但不要和 Nano（`100`）冲突。

---

### 步骤 2：添加远程设备（Nano）

1. 左侧项目树 → **通讯连接** → **网络 PLC** 选项卡
2. 点击 **新增**
3. 填入以下参数：

| 参数 | 值 |
|------|-----|
| 制造商 | MODBUS 网络兼容设备 |
| 设备类型 | Modbus TCP |
| 设备别名 | Nano（可自定义） |
| IP 地址 | `192.168.2.100` |
| 端口号 | `502` |
| 预设站号 | `1` |

4. 点击确定保存

---

### 步骤 3：配置通信变量（标签）

在 **网络 PLC** 配置界面中，需要为每个寄存器添加通信标签。

#### 标签 1：触发信号（写 HR0）

| 参数 | 值 |
|------|-----|
| 功能码 | 06 写单个寄存器 |
| 从站寄存器地址 | 0 |
| 本地变量 | D0 |

#### 标签 2：检测结果（读 HR1）

| 参数 | 值 |
|------|-----|
| 功能码 | 03 读保持寄存器 |
| 从站寄存器地址 | 1 |
| 本地变量 | D1 |

#### 标签 3：系统状态（读 HR2）

| 参数 | 值 |
|------|-----|
| 功能码 | 03 读保持寄存器 |
| 从站寄存器地址 | 2 |
| 本地变量 | D2 |

#### 标签 4：完成标志（读 HR3，读到 1 再取结果）

| 参数 | 值 |
|------|-----|
| 功能码 | 03 读保持寄存器 |
| 从站寄存器地址 | 3 |
| 本地变量 | D3 |

> D3 只用于**判断**：读到 1 = Nano 已写好结果，可以取 HR1。HR3 由 Nano 清 0，PLC 不需要也不能写它。

#### 标签 5：应答（写 HR4，取走结果后写 1）

| 参数 | 值 |
|------|-----|
| 功能码 | 06 写单个寄存器 |
| 从站寄存器地址 | 4 |
| 本地变量 | D4 |

> ⚠️ **应答写 HR4，不写 HR3**。读（标签4 读 HR3→D3）和写（标签5 写 HR4←D4）用不同寄存器、
> 不同本地变量，方向互不干扰，FStudio 不会出现"同一个变量又被读轮询又被写"的覆盖冲突。
> D4 只由程序写：1=应答、0=待命。Nano 会自己复位 HR4，所以 D4 写标签的触发方式随意（持续写/变化写都行）。

> **地址格式说明**：如果填 0、1、2 通信失败，尝试改用 40001、40002、40003（FStudio 不同版本地址格式可能不同）。

---

### 步骤 4：导入变量到全局变量表

1. 在网络 PLC 配置界面，点击 **增量导入全局变量**
2. 勾选 D0、D1、D2 等需要通信的变量
3. 点击确定，变量将出现在 **全局变量表** 中

---

### 步骤 5：编写 PLC 程序（梯形图 / ST）

以 ST（结构化文本）为例：

```
// ========================================
// 触发拍照：上一板结果被取走(HR3=0) 才允许触发
// ========================================
IF 工件到位传感器 AND D3 = 0 THEN
    D0 := 1;          // 写 HR0=1，触发 Nano 拍照
ELSE
    D0 := 0;          // 结果没取走前不再触发，防覆盖
END_IF;

// ========================================
// 取结果：看到完成标志 HR3=1 才读，不用等 300ms
// ========================================
IF D3 = 1 THEN
    CASE D1 OF
        1:  // OK → 放行
            剔除电磁阀 := FALSE;
        2:  // NG → 剔除
            剔除电磁阀 := TRUE;
    END_CASE;
    D4 := 1;          // 应答：写 HR4=1（标签5），告诉 Nano 结果已取走
END_IF;

// ========================================
// 重武装：Nano 清掉完成标志(HR3=0)后，应答变量复位回待命
// ========================================
IF D3 = 0 AND D4 = 1 THEN
    D4 := 0;          // 待命，下次应答再走 0→1
END_IF;
```

> D1、D2、D3 由标签读轮询自动更新；D4 只由程序写（1=应答，0=待命），经标签5 发 HR4。
> 读（HR3）写（HR4）分开在不同寄存器，不会出现读轮询覆盖应答的冲突。

---

## 四、调试

### 4.1 查看 Nano 日志

正常连接时，Nano 终端输出：

```
[PLC] Modbus TCP Server 已启动, 端口=502, 从站ID=1, 等待 PLC 连接...
[PLC] Modbus TCP 客户端已连接
[PLC] 收到触发指令
[PLC] → OK
[PLC] → NG
```

### 4.2 查看 FC5 通信日志

FStudio → **在线** → **通信日志**，观察是否有超时报错。

### 4.3 常见问题排查

| 现象 | 可能原因 | 排查方法 |
|------|---------|---------|
| PLC 连不上 Nano | 网线不通 / IP 不在同一网段 | PLC 和 Nano 互相 ping 对方 IP |
| 通信日志报超时 | 端口号不对 / Nano 程序没启动 | Nano 上 `netstat -tlnp \| grep 502` 检查是否监听 |
| 寄存器读出来一直是 0 | 地址格式不对（0 vs 40001） | 换另一种地址格式试试 |
| 无法绑定 502 端口 | 非 root 用户 | `sudo setcap 'cap_net_bind_service=+ep' ./wood_defect_detector` |

---

## 五、完整通信时序

```
PLC（FC5）                           Nano
   │                                   │
   │──── 写 HR0 = 1 ──────────────────→│  工件到位，触发拍照
   │                                   │  拍照 → AI 检测（耗时多少都行）
   │                                   │  sendOK()/sendNG() → HR1 = 1 或 2
   │                                   │  setDone() → HR3 = 1（结果已写好，钉住）
   │←─── 轮询读到 HR3 = 1 ────────────│  PLC 每轮扫表都能看到，不抢窗口
   │←─── 读 HR1 = 1 (OK) ─────────────│  取结果
   │  放行 / 剔除                       │
   │──── 写 HR4 = 1（应答）───────────→│  Nano 看到 HR4 0→1，清 HR3、复位 HR4，接下一板
   │  下一个工件到位...                  │
```

> **不是定时判断，是握手**：PLC 等到 HR3=1 才取结果，取完写 HR4=1 应答。
> 只要 HR3 是 1（结果没取走），Nano 就不接新触发；只要 HR3 是 0，PLC 就不取结果。
> 两边互相等信号，没有任何一方需要卡毫秒。

---

## 六、调试命令速查

### 6.1 网络连通性

```bash
# 查看 Nano 有线网卡 IP
ifconfig enP8p1s0

# ping 相机
ping 192.168.2.10

# ping PLC
ping 192.168.2.200

# 扫描整个网段在线设备（sudo 才能扫全）
sudo nmap -sn 192.168.2.0/24

# 抓 ARP 包确认 PLC 的真实 IP（PLC 上电时抓）
sudo tcpdump -i enP8p1s0 arp -nn

# 抓 Modbus TCP 包，分析 PLC 写的地址和值
sudo tcpdump -i enP8p1s0 port 502 -X -nn
```

### 6.2 Modbus TCP 调试

```bash
# 查看 Nano 502 端口是否在监听
ss -tlnp | grep 502

# 测试 TCP 连接
nc -zv 192.168.2.100 502

# 用 mbpoll 读寄存器（-r 1 = HR0, -r 2 = HR1, -r 3 = HR2, -r 4 = HR3, -r 5 = HR4）
# 注意：需要先关掉程序，否则 PLC 占着连接，mbpoll 连不上
mbpoll -m tcp -a 1 -r 1 -c 5 192.168.2.100

# 用 mbpoll 模拟 PLC 写 HR0=1 触发拍照
mbpoll -m tcp -a 1 -r 1 192.168.2.100 1
```

### 6.3 502 端口权限问题

端口 502 是特权端口（< 1024），需要授权才能绑定：

```bash
# 给可执行文件授权
sudo setcap 'cap_net_bind_service=+ep' ./build/wood_defect_detector

# 确认授权生效
getcap ./build/wood_defect_detector
```

> **原理**：Linux 安全机制规定 1024 以下的端口只有 root 才能用。502 < 1024，普通用户启动会报 `Permission denied`。
> `setcap` 给**这一个可执行文件**单独授权 `cap_net_bind_service`，允许它绑定特权端口，不需要整个程序跑在 root 下。
>
> **注意**：权限绑在文件上，每次 `cmake --build` 生成新二进制后权限就没了，需要重新跑一次 `setcap`。

未授权时启动会报错：
```
[PLC] modbus_tcp_listen 失败: Permission denied
```

### 6.4 抓包分析示例

PLC 写 HR0=1 的正确 Modbus 报文（功能码 06 = 写单个寄存器）：
```
... 0000 0006 0106 0000 0001 ...
                  ^^^^ ^^^^
                  地址0  值=1
```

> **关键**：代码 `waitTrigger` 检查的是 `tab_registers[0] == 1`，**值必须等于 1** 才触发。
> 如果 PLC 写的是 11（0x000B）等其他值，不会触发拍照。
> 地址 0 = HR0，地址 1 = HR1，地址 2 = HR2。确认 FStudio 里触发标签的**从站寄存器地址**填的是 `0`（或 `40001`）。

### 6.5 程序运行正常标志

Modbus TCP Server 正常启动后，终端输出：
```
[PLC] Modbus TCP Server 已启动, 端口=502, 从站ID=1, 等待 PLC 连接...
[PLC] Modbus TCP 客户端已连接          ← PLC 连上来会出现
[PLC] → OK  或  [PLC] → NG            ← 收到触发后拍照完成
```

### 6.6 工具安装

```bash
sudo apt install nmap -y
sudo apt install tcpdump -y
sudo apt install mbpoll -y
```

### 6.7 CIDR /24 说明

`192.168.2.0/24` 里的 `/24` 对应子网掩码 `255.255.255.0`，表示扫描 `192.168.2.0` ~ `192.168.2.255` 共 256 个地址，刚好覆盖整个局域网。
