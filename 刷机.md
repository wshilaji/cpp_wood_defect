# Jetson Orin Nano Super 刷机（NVMe 烧录）

版本：**JetPack 6.2.1 (L4T R36.4.4)**，已确认支持 Orin Nano Super 模式。

---

## 一、下载两个 BSP 文件

| 文件 | 说明 |
|------|------|
| `Jetson_Linux_R36.4.4_aarch64.tbz2` | L4T 驱动包 (BSP) |
| `Tegra_Linux_Sample-Root-Filesystem_R36.4.4_aarch64.tbz2` | 示例根文件系统 |

```bash
wget https://developer.nvidia.com/downloads/embedded/l4t/r36_release_v4.4/release/jetson_linux_r36.4.4_aarch64.tbz2
wget https://developer.nvidia.com/downloads/embedded/l4t/r36_release_v4.4/release/tegra_linux_sample-root-filesystem_r36.4.4_aarch64.tbz2
```

---

## 二、解压并准备

替换掉电脑上旧的 `Linux_for_Tegra` 文件夹：

```bash
# 解压驱动包（文件名与 wget 下载的小写名一致，Linux 大小写敏感）
tar xf jetson_linux_r36.4.4_aarch64.tbz2
cd Linux_for_Tegra

# 将根文件系统解压到指定目录
sudo tar xpf ../tegra_linux_sample-root-filesystem_r36.4.4_aarch64.tbz2 -C rootfs/

# 应用二进制文件（把根文件系统和硬件驱动打包到一起）
sudo ./apply_binaries.sh
```

> `apply_binaries.sh` 成功后，`Linux_for_Tegra` 目录就准备好刷机所需的全部文件了。

---

## 三、完整刷机流程

### 1. 检查是否成功

脚本执行完，终端会显示类似 `Successfully generated...` 或 `Done` 的提示，并且不会报错。

### 2. 确认设备已进入恢复模式

用 USB-C 数据线连接 Jetson 和电脑：

```bash
lsusb | grep NVIDIA
```

看到类似 `0955:7523 NVIDIA Corp. APX` 的输出，说明设备已正确连接并处于恢复模式。

### 3. 执行 NVMe 刷机命令

在 `Linux_for_Tegra` 目录下运行（指定 `jetson-orin-nano-devkit-super`）：

```bash
sudo ./tools/kernel_flash/l4t_initrd_flash.sh --external-device nvme0n1p1 \
  -c tools/kernel_flash/flash_l4t_t234_nvme.xml -p "-c bootloader/generic/cfg/flash_t234_qspi.xml" \
  --showlogs --network usb0 jetson-orin-nano-devkit-super internal
```

---

## 四、常见报错

### `Unrecognized module SKU`

说明 R36.4.4 可能仍无法识别这块 "Super" 板子。可尝试：

- 换 **R36.5.0** 版本（社区反馈该版本可成功刷入 Super 模式）
- 或参考论坛方法手动修改配置文件

### Secureboot package 相关错误

在命令中添加 `--no-secureboot` 参数跳过检查：

```bash
sudo ./tools/kernel_flash/l4t_initrd_flash.sh --no-secureboot --external-device nvme0n1p1 \
  -c tools/kernel_flash/flash_l4t_t234_nvme.xml -p "-c bootloader/generic/cfg/flash_t234_qspi.xml" \
  --showlogs --network usb0 jetson-orin-nano-devkit-super internal
```
